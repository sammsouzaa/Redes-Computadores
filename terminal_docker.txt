docker pull darlon/ssh //puxa os arquivos do ssh do darlon

docker run --rm -d --name ssh -p 2222:22 darlon/ssh //cria um servidor ssh rodando na maquina

docker ps //verifica se o servidor ssh esta rodando

ssh -p 2222 samu@localhost //entra dentro do servidor ssh

scp -P 2222 /home/aluno/redes/hehe.jpg samu@localhost:/tmp/ //coloca o arquivo dentro do servidor ssh

ls /tmp //mostra os arquivos existentes dentro do tmp

scp -P 2222 /tmp/hehe.jpg samu@192.168.246.55:/tmp/hehe.jpg //copia o arquivo dentro de um ssh para outro ssh

scp -P 2222 samu@192.168.246.60:/tmp/mosquito.jpg . //dentro da maquina pc, faz download de um arquivo dentro de um servidor ssh
